{
  "name": "fuzzforge",
  "private": true,
  "type": "module",
  "scripts": {
    "build:verifier": "cargo build --manifest-path worker/verifier/Cargo.toml --release --target wasm32-unknown-unknown && mkdir -p worker/generated && cp worker/verifier/target/wasm32-unknown-unknown/release/fuzzforge_worker_verifier.wasm worker/generated/verifier.wasm",
    "test": "cargo test && npm run typecheck && npm run test:worker",
    "test:worker": "npm run build:verifier && vitest run",
    "typecheck": "npm run build:verifier && tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.69",
    "@cloudflare/workers-types": "^4.20260612.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.21.0"
  }
}
